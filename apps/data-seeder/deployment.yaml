apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-seeder
  namespace: airbnb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-seeder
  template:
    metadata:
      labels:
        app: data-seeder
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: data-seeder
          image: ghcr.io/my-airbnb/data-seeder:dev-9fb8763b860fe6f2d3a391406301c196fd8e503a
          ports:
            - containerPort: 8086
          envFrom:
            - configMapRef:
                name: data-seeder-config
            - secretRef:
                name: data-seeder-secret
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "768Mi"
              cpu: "300m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8086
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 10
