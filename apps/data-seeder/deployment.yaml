apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-seeder
  namespace: airbnb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-seeder
  template:
    metadata:
      labels:
        app: data-seeder
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: data-seeder
          image: ghcr.io/my-airbnb/data-seeder:dev-182e8dae70a973c4d9b99a9d87b13ee7a22407ed
          ports:
            - containerPort: 8086
          envFrom:
            - configMapRef:
                name: data-seeder-config
            - secretRef:
                name: data-seeder-secret
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "768Mi"
              cpu: "300m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8086
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 10
