apiVersion: v1
kind: Service
metadata:
  name: service-recently-viewed
  namespace: airbnb
spec:
  selector:
    app: service-recently-viewed
  ports:
    - port: 8091
      targetPort: 8091
  type: ClusterIP
