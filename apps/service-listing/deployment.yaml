apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-listing
  namespace: airbnb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-listing
  template:
    metadata:
      labels:
        app: service-listing
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: service-listing
          image: ghcr.io/my-airbnb/service-listing:dev-c26cfdbab2e618c556a3723a9411dea5a6114750
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: service-listing-config
            - secretRef:
                name: service-listing-secret
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 5
