# TEMPLATE - seal with: kubeseal --cert pub-cert.pem < secret.yaml > sealed-secret.yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: service-auth-secret
  namespace: airbnb
spec:
  encryptedData:
    SPRING_DATASOURCE_PASSWORD: <SEALED>
    JWT_SECRET: <SEALED>
