apiVersion: v1
kind: Service
metadata:
  name: service-notification
  namespace: airbnb
spec:
  selector:
    app: service-notification
  ports:
    - port: 8088
      targetPort: 8088
  type: ClusterIP
