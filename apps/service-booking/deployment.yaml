apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-booking
  namespace: airbnb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-booking
  template:
    metadata:
      labels:
        app: service-booking
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: service-booking
          image: ghcr.io/my-airbnb/service-booking:dev-39c51cd31b38df664bce69003803edd9c191d47b
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: service-booking-config
            - secretRef:
                name: service-booking-secret
            - secretRef:
                name: rabbitmq-app-secret
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 5
