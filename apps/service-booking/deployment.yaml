apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-booking
  namespace: airbnb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-booking
  template:
    metadata:
      labels:
        app: service-booking
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: service-booking
          image: ghcr.io/my-airbnb/service-booking:dev-a623fd2e8a6519f2908afd4117e8b6b854eeb407
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: service-booking-config
            - secretRef:
                name: service-booking-secret
            - secretRef:
                name: rabbitmq-app-secret
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 5
